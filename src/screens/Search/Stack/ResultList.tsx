import { Text, View } from "react-native";

const ResultList: React.FC = () => {
  return (
    <View style={{ flex: 1, alignItems: "center", justifyContent: "center" }}>
      <Text>ResultList Screen</Text>
    </View>
  );
};

export default ResultList;
